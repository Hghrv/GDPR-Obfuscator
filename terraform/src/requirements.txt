# pycryptodome  # This is library could be also be used by devolopers to add further levels of masking security if needed or to mask to whole code itself.
pytest==8.4.2    # For unit-testing
pytest-cov==7.0.0   # Coverage plugin
pytest-flake8==1.3.0   # Flake8 plugin
testfixtures==10.0.0
autopep8==2.3.2 # PEP* Formatting
safety==3.6.2   # Safety scan
# python-terraform==1.13.3    # Install required version with <sudo apt install terraform=1.13.3-1> instead (pip still at v0.9)
boto3==1.40.40  # For s3 client connection
awscli==1.42.49   # CLI for AWS
botocore==1.40.49   # For Boto3
moto==5.1.13   # For mock tests   
pandas==2.3.3   # For Dataframe processing
pyarrow==22.0.0
s3fs    # For AWS S3 compatibility with Pandas
git-lfs # For eventual large zip push handling
make    # For automation